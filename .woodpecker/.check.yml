pipeline:
  install:
    image: node
    commands:
      - npm install --unsafe-perm
  build:
    image: node
    commands:
      - npm run build
  lint:
    image: node
    group: test
    commands:
      - npm run lint
  test:
    image: node
    group: test
    commands:
      - npm run test
